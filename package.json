{
  "name": "line-api-test-worker",
  "version": "1.0.0",
  "description": "A Cloudflare Worker to test and understand the LINE Messaging API.",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
